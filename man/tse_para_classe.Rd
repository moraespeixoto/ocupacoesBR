% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tse.R
\name{tse_para_classe}
\alias{tse_para_classe}
\title{Classe social da ocupação declarada ao TSE}
\usage{
tse_para_classe(cod, superior = NULL, ano = NULL)
}
\arguments{
\item{cod}{Vetor de códigos de ocupação do TSE (numérico ou texto).}

\item{superior}{Vetor lógico opcional, do mesmo comprimento de \code{cod},
indicando ensino superior completo. Quando fornecido, a categoria residual
"Vínculo público não especificado" — que sozinha reúne cerca de 9\% das
candidaturas, do faxineiro ao secretário de finanças — é dividida pela
escolaridade declarada.}

\item{ano}{Vetor opcional de anos de eleição, do mesmo comprimento de \code{cod}.
Com ele, as candidaturas cujo código o TSE \strong{reutilizou} depois voltam
\code{NA} com aviso, em vez de traduzidas pelo dicionário errado: o código 215
designava um cargo de direção até 2000 e passou a designar artista
plástico. Sem ele, o comportamento é o de sempre. Veja \code{\link[=tse_vigencia]{tse_vigencia()}}.}
}
\value{
Vetor de texto com a classe.
}
\description{
Esquema de doze categorias desenhado para o dado eleitoral brasileiro. Não é
o EGP: parte do ISCO como o EGP parte, mas trata como categorias próprias
duas situações que o registro eleitoral cria e que os esquemas canônicos não
preveem — proprietários e empregadores nomeados pelo próprio código de
ocupação, e o vínculo público declarado sem função especificada. Para o EGP
canônico, veja \code{\link[=tse_para_egp]{tse_para_egp()}}.
}
\section{O agricultor, e por que ele não tem classe própria}{

Os códigos 601 (agricultor) e 604 (pescador) ficam em \code{"Trabalhadores rurais"}, portanto nas classes populares, embora \code{\link[=tse_para_egp]{tse_para_egp()}} os
classifique como \verb{IVc: proprietário rural}. A aparente discordância entre as
duas medidas é \strong{artefato de granularidade}, e o registro é útil porque a
classe própria chegou a ser criada, em 29/07/2026, e foi revertida no mesmo
dia.

O esquema relacional só separa IVc de VIIb nas \strong{onze} classes. Nos colapsos
canônicos de Erikson e Goldthorpe, o agricultor e o assalariado rural voltam
a ser a mesma coisa:\tabular{lll}{
   \code{n_classes} \tab 601 agricultor \tab 606 assalariado rural \cr
   11 \tab IVc \tab VIIb \cr
   5 \tab IVc+VIIb \tab IVc+VIIb \cr
   3 \tab Agrícolas \tab Agrícolas \cr
}


Comparar um esquema de onze classes com uma partição em três estratos é
comparar resoluções diferentes, não encontrar divergência. Na resolução
equivalente à do estrato, o próprio EGP funde os dois.

O dado externo concorda. O ISEI do agricultor, 23, está \strong{dentro} da faixa
das classes populares, que vai de 16 a 43, e corresponde ao percentil 5 do
dicionário. O patrimônio mediano declarado, de R$ 254.582, fica acima do
máximo das classes populares por menos de três mil reais: põe o agricultor no
topo da classe popular, não fora dela.

A distinção entre agricultura familiar e proletariado rural é real, e continua
disponível onde Erikson e Goldthorpe a puseram: em
\code{tse_para_egp(n_classes = 11)}. O que não se justifica é transportá-la para a
partição em estratos.
}

\examples{
tse_para_classe(c(111, 169, 291))
tse_para_classe(c(291, 291), superior = c(TRUE, FALSE))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cod.R
\name{crosswalk_cod}
\alias{crosswalk_cod}
\title{Tabela completa de traducao a partir da COD}
\usage{
crosswalk_cod(cod = NULL)
}
\arguments{
\item{cod}{Vetor opcional de codigos. Se omitido, devolve a tabela inteira.}
}
\value{
\code{data.frame} com COD, titulo, ISCO-08, ISCO-88, as duas versoes do
ISEI, o prestigio, o EGP e o tipo de correspondencia.
}
\description{
Tabela completa de traducao a partir da COD
}
\section{A coluna \code{egp} sai degradada, e em silêncio}{

O EGP não é função só da ocupação: as suas regras pedem a posição no emprego
e o número de subordinados, e nenhum dos dois existe num código de COD da PNAD. A
coluna é calculada sem eles, e a consequência não é ruído — \strong{IVa, IVb e V
saem estruturalmente vazias}, de modo que a pequena burguesia aparece
contada como classe de serviço. Isso é inversão de classe, não
arredondamento.

A chamada silencia o aviso que \code{\link[=isco88_para_egp]{isco88_para_egp()}} emitiria, porque repeti-lo
uma vez por linha seria inútil; a ressalva passou a viver aqui, na auditoria
de 05/09/2026. Para publicar, use os colapsos de 5 ou 3 classes, onde IVa e
IVb se fundem a categorias que existem, e considere \link{isco_posicao_br} como
prior empírico da posição no emprego. Compare com \code{\link[=crosswalk_tse]{crosswalk_tse()}}, que é
a única das quatro tabelas em que o EGP sai completo, porque o dicionário
do TSE marca quem trabalha por conta própria.
}

\examples{
head(crosswalk_cod())
crosswalk_cod(c("2211", "0411", "6225"))
}

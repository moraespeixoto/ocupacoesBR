% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbo.R
\name{crosswalk_cbo2002}
\alias{crosswalk_cbo2002}
\title{Tabela completa de traducao a partir da CBO-2002}
\usage{
crosswalk_cbo2002(cbo = NULL, empate = c("na", "moda"), escada = FALSE)
}
\arguments{
\item{cbo}{Vetor opcional de codigos. Se omitido, devolve a tabua inteira.}

\item{empate}{O que fazer quando a familia nao tem ISCO majoritario:
\code{"na"} (padrao) devolve \code{NA}; \code{"moda"} devolve o vencedor do desempate
por menor codigo, como nas versoes anteriores.}

\item{escada}{Se \code{TRUE}, sobe a hierarquia da CBO para preencher as ocupacoes
ausentes da tabua; veja \code{\link[=cbo2002_para_isco]{cbo2002_para_isco()}}. A coluna \code{nivel_usado} sai
de qualquer modo, e diz de que degrau veio o ISCO de cada linha: 6 e a
propria ocupacao, 4 a familia, 3 o subgrupo, 2 o subgrupo principal.}
}
\value{
\code{data.frame} com CBO-2002, titulo, familia, CBO-94, ISCO-88, ISCO-08,
ISEI, SIOPS, EGP, \code{agregado}, \code{empate} e \code{nivel_usado}. \code{empate} e \code{TRUE}
quando a linha e uma familia de quatro digitos sem ISCO majoritario; com
\code{empate = "na"} (padrao) o ISCO dessa linha sai \code{NA}, exatamente como em
\code{\link[=cbo2002_para_isco]{cbo2002_para_isco()}} — a tabela nao pode devolver o que a porta recusa.
}
\description{
Tabela completa de traducao a partir da CBO-2002
}
\section{A coluna \code{egp} sai degradada, e em silêncio}{

O EGP não é função só da ocupação: as suas regras pedem a posição no emprego
e o número de subordinados, e nenhum dos dois existe num código de CBO-2002. A
coluna é calculada sem eles, e a consequência não é ruído — \strong{IVa, IVb e V
saem estruturalmente vazias}, de modo que a pequena burguesia aparece
contada como classe de serviço. Isso é inversão de classe, não
arredondamento.

A chamada silencia o aviso que \code{\link[=isco88_para_egp]{isco88_para_egp()}} emitiria, porque repeti-lo
uma vez por linha seria inútil; a ressalva passou a viver aqui, na auditoria
de 05/09/2026. Para publicar, use os colapsos de 5 ou 3 classes, onde IVa e
IVb se fundem a categorias que existem, e leia \code{\link[=isco88_para_egp]{isco88_para_egp()}}.
}

\examples{
head(crosswalk_cbo2002())
crosswalk_cbo2002(c("1111-05", "225120"))
}

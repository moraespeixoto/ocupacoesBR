% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{checa_cobertura}
\alias{checa_cobertura}
\title{Verifica se todos os códigos observados estão no dicionário}
\usage{
checa_cobertura(cod, silencioso = FALSE)
}
\arguments{
\item{cod}{Vetor de códigos de ocupação observados no seu dado.}

\item{silencioso}{Se \code{TRUE}, não escreve a mensagem de sucesso.}
}
\value{
Invisivelmente, \code{TRUE}. Erro se houver código fora do dicionário ou
se não houver código válido algum.
}
\description{
Falha com erro se algum código de ocupação presente no seu dado não tiver
entrada no dicionário do pacote, \strong{ou se não houver código nenhum}. Chame
antes de qualquer análise.
}
\section{Por que isto existe}{

O modo silencioso de errar uma medida de classe não é classificar mal um
caso: é um código novo cair num rótulo residual sem que ninguém perceba. Foi
o que aconteceu na primeira versão deste dicionário, em que 76 códigos reais
caíam no rótulo "fora da PEA" — inclusive proprietários, que somem
justamente da categoria em que mais importam.

O segundo modo silencioso é mais banal e mais comum: passar a coluna errada.
\code{dados$CD_OCUPACAO} num quadro cuja coluna se chama \code{cd_ocupacao} devolve
\code{NULL}, e uma verificação ingênua aprovaria o vetor vazio. Por isso entrada
vazia, \code{NULL} ou inteiramente ausente também é erro aqui.
}

\examples{
checa_cobertura(c(111, 169, 257))
try(checa_cobertura(c(111, 99999)))
try(checa_cobertura(NULL))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cod.R
\name{cod_para_egp}
\alias{cod_para_egp}
\title{Classe EGP a partir da COD}
\usage{
cod_para_egp(
  cod,
  conta_propria = NULL,
  n_supervisionados = NULL,
  n_classes = 11,
  rotulo = TRUE,
  avisar = TRUE
)
}
\arguments{
\item{cod}{Vetor de codigos da COD, de quatro digitos.}

\item{conta_propria}{Vetor lógico opcional: a pessoa trabalha por conta
própria ou é empregadora (\code{TRUE}) ou é empregada (\code{FALSE}).}

\item{n_supervisionados}{Vetor numérico opcional com o número de pessoas
supervisionadas.}

\item{n_classes}{Número de classes do resultado: 11 (padrão), 7, 5 ou 3.}

\item{rotulo}{Se \code{TRUE} (padrão), devolve rótulos — em todas as versões,
inclusive nas colapsadas; se \code{FALSE}, os códigos inteiros.}

\item{avisar}{Se \code{TRUE} (padrão), avisa quando \code{conta_propria} ou
\code{n_supervisionados} não são fornecidos.}
}
\value{
Vetor de texto (ou inteiro, se \code{rotulo = FALSE}).
}
\description{
A PNAD Continua tem posicao na ocupacao (V4012) e o numero de empregados do
empregador, o que recupera a pequena burguesia e \strong{separa IVa de IVb} — a
distincao que pela porta do TSE fica indeterminada. Passe-os.
}
\details{
O que ela nao tem e a supervisao exercida sobre assalariados, que e o que
define V (supervisores manuais) e governa as promocoes entre I e II. Por essa
porta o EGP fica mais completo que pelas outras, mas nao completo.
}
\examples{
cod_para_egp("2211", avisar = FALSE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotulos.R
\name{tse_rotulo_para_cod}
\alias{tse_rotulo_para_cod}
\title{Encontra o código a partir do rótulo da ocupação}
\usage{
tse_rotulo_para_cod(rotulo, exato = TRUE)
}
\arguments{
\item{rotulo}{Vetor de rótulos de ocupação.}

\item{exato}{Se \code{TRUE} (padrão), só casa o rótulo inteiro; se \code{FALSE}, casa
por conteúdo e pode devolver mais de um código por rótulo.}
}
\value{
\code{data.frame} com \code{rotulo} (o que se procurou), \code{cod_tse} e o rótulo
canônico encontrado.
}
\description{
Serve a quem recebe a ocupação como texto e não como código — o caso de quem
baixa \code{br_tse_eleicoes.candidatos} no \code{basedosdados}. A busca ignora caixa,
acento e pontuação.
}
\examples{
tse_rotulo_para_cod("Agricultor")
tse_rotulo_para_cod("advogado", exato = FALSE)
}

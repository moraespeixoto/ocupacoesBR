% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/egp.R
\name{tse_para_egp}
\alias{tse_para_egp}
\title{Classe EGP da ocupação declarada ao TSE}
\usage{
tse_para_egp(
  cod,
  conta_propria = NULL,
  n_supervisionados = NULL,
  n_classes = 11,
  rotulo = TRUE,
  avisar = TRUE,
  usa_proprietario = TRUE,
  ano = NULL
)
}
\arguments{
\item{cod}{Vetor de códigos de ocupação do TSE (numérico ou texto).}

\item{conta_propria}{Vetor lógico opcional: a pessoa trabalha por conta
própria ou é empregadora (\code{TRUE}) ou é empregada (\code{FALSE}).}

\item{n_supervisionados}{Vetor numérico opcional com o número de pessoas
supervisionadas.}

\item{n_classes}{Número de classes do resultado: 11 (padrão), 7, 5 ou 3.}

\item{rotulo}{Se \code{TRUE} (padrão), devolve rótulos — em todas as versões,
inclusive nas colapsadas; se \code{FALSE}, os códigos inteiros.}

\item{avisar}{Se \code{TRUE} (padrão), avisa quando \code{conta_propria} ou
\code{n_supervisionados} não são fornecidos.}

\item{usa_proprietario}{Se \code{TRUE} (padrão) e \code{conta_propria} não for
informado, usa a marca \code{proprietario} do dicionário como posição no
emprego. Informar \code{conta_propria} explicitamente tem precedência.}

\item{ano}{Vetor opcional de anos de eleição, do mesmo comprimento de \code{cod}.
Com ele, as candidaturas cujo código o TSE \strong{reutilizou} depois voltam
\code{NA} com aviso, em vez de traduzidas pelo dicionário errado: o código 215
designava um cargo de direção até 2000 e passou a designar artista
plástico. Sem ele, o comportamento é o de sempre. Veja \code{\link[=tse_vigencia]{tse_vigencia()}}.}
}
\value{
Vetor de texto (ou inteiro, se \code{rotulo = FALSE}).
}
\description{
Atalho de \code{\link[=tse_para_isco]{tse_para_isco()}} seguido de \code{\link[=isco88_para_egp]{isco88_para_egp()}}. Leia
\code{\link[=isco88_para_egp]{isco88_para_egp()}} antes de usar: partindo só do TSE, o EGP sai incompleto,
porque o formulário não pergunta supervisão.
}
\section{A posição no emprego que o dicionário conhece}{

O TSE não pergunta posição na ocupação, mas alguns dos seus códigos
\strong{nomeiam} no próprio rótulo quem trabalha por conta própria — comerciante,
empresário, pecuarista, proprietário de estabelecimento, e também o
agricultor e o pescador. O dicionário marca essa condição em
\code{tse_isco$conta_propria}, e é ela, não \code{proprietario}, que corresponde ao
\code{SEMPL = 2} das sintaxes do ISMF. As duas marcas são distintas desde
07/2026: \code{proprietario} é o subconjunto que pertence à classe proprietária,
e o agricultor (601) e o pescador (604) estão fora dele sem deixarem de
trabalhar por conta própria. Enquanto eram uma coisa só, os dois caíam em
VIIb, o fundo do esquema, ao lado do assalariado rural.

Por padrão (\code{usa_proprietario = TRUE} — o nome do argumento é anterior à
separação das duas marcas e foi mantido por compatibilidade) a função a
utiliza. São doze os códigos marcados, e sete deles mudam de classe por
causa dela. Cinco — 169, 902, 903, 904 e 905 — sairiam em II, a classe de
serviço assalariada, o que é uma inversão: a pequena burguesia contada como
classe de serviço. Os outros dois — 601 e 604 — sairiam em VIIb, ao lado do
assalariado rural.

O que \strong{continua} indeterminado é a divisão entre IVa (conta própria com
empregados) e IVb (sem), que exige o número de subordinados. Todos caem em
IVb. Para publicar, prefira \code{n_classes = 7} ou \code{5}, onde as duas se fundem
em IVab.
}

\examples{
# 169 é COMERCIANTE: pequena burguesia, não classe de serviço
tse_para_egp(c(111, 169, 601), avisar = FALSE)
tse_para_egp(169, usa_proprietario = FALSE, avisar = FALSE)
}
